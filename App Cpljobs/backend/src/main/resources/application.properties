# DataSource settings
spring.datasource.url=jdbc:mysql://localhost:3306/cpl
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# Specify the DBMS
spring.jpa.database=MYSQL

# Show or not log for each SQL query
spring.jpa.show-sql=true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update

# Disable failing on empty beans during Jackson serialization
spring.jackson.serialization.fail-on-empty-beans=false

# Servlet multipart configuration
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB

spring.mail.host=smtp.office365.com
spring.mail.port=587
spring.mail.username=chouaibiaymen2023@outlook.com
spring.mail.password=rvzzhaztygugplei
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true



# JWT Configuration
auth.secret= "security1"
auth.expiration= 60480
refresh-token.expiration=604800
