# DataSource settings
spring.datasource.url=jdbc:mysql://localhost:3306/sirh
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# Specify the DBMS
spring.jpa.database=MYSQL

# Show or not log for each SQL query
spring.jpa.show-sql=true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update

# Disable failing on empty beans during Jackson serialization
spring.jackson.serialization.fail-on-empty-beans=false

# Servlet multipart configuration
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB

# Email configuration for Outlook
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=belhassen.bouhalfaya@ult-tunisie.com
spring.mail.password=toutou123

# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true
